node_modules
dist
.env
*.log
screenshots/
qa-shots/
output/
.gev-logs/
.gev-cache/
.DS_Store
.gstack/
3d-models/
pinokio/ENVIRONMENT
pinokio/.installed

# Provider credential files (OpenSky OAuth client JSON, etc.)
*credentials*.json
*credential*.json

# OMC operational state
.omc/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
